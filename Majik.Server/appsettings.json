{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Auth": {
    "_comment": "Descope project. Authority = 'https://api.descope.com/<PROJECT_ID>'. Audience = <PROJECT_ID>. Empty Authority = auth disabled (dev convenience before IdP provisioned).",
    "Authority": "",
    "Audience": ""
  },
  "Mongo": {
    "_comment": "MongoDB connection for new entities (profiles, decks, matches). Empty ConnectionString = endpoints return 503.",
    "ConnectionString": "",
    "Database": "majik-dev"
  }
}
