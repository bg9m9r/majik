{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Auth": {
    "_comment": "Auth0 tenant + API identifier. Authority requires trailing slash. Empty = auth disabled (dev convenience before IdP provisioned).",
    "Authority": "",
    "Audience": ""
  }
}
